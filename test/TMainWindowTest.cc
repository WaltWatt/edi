#include <gtest/gtest.h>

#include "../src/ui/tui/TMainWindow.h"

using namespace tui;

/*
TEST(TMainWindow, Construction)
{
	TMainWindow mainWindow;
	ASSERT_TRUE(sizeof(mainWindow) > 1);
}

TEST(TMainWindow, GetMainWindowSize)
{
	TMainWindow mainWindow;
	ASSERT_EQ(5, 5);
	ASSERT_EQ(0, mainWindow.width());
}
*/
